<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pract.es.deusto.wake_up">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registro" />
        <activity android:name=".List_Pacientes" />
        <activity
            android:name=".Menu_Lateral"
            android:label="@string/title_activity_menu__lateral"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewUser" />
        <activity android:name=".Medicion" />
        <activity android:name=".MySettings"></activity>
    </application>

</manifest>